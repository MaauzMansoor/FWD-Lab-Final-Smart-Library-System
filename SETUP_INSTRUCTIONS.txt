# SETUP AND RUN INSTRUCTIONS
# Smart Library System - MERN Stack

## ✅ PROJECT STRUCTURE

FED-LAB-FINAL/
├── client/          # React frontend
├── server/          # Node.js backend
├── README.md        # Project documentation
├── .gitignore       # Git ignore rules
└── package.json     # Root package file


## 📦 STEP 1: INSTALL DEPENDENCIES

### Backend Setup:
1. Open terminal
2. Navigate to server directory:
   cd "e:\4th semester tasks\FED-LAB-FINAL\server"

3. Install dependencies:
   npm install

   This installs:
   - express (server framework)
   - mongoose (MongoDB ODM)
   - cors (cross-origin requests)
   - dotenv (environment variables)


### Frontend Setup:
1. Navigate to client directory:
   cd "e:\4th semester tasks\FED-LAB-FINAL\client"

2. Install dependencies (already done):
   npm install

   This installs:
   - react & react-dom
   - axios (HTTP client)
   - All CRA dependencies


## 🚀 STEP 2: RUN THE APPLICATION

### Method 1: Run Both Servers Separately

#### Terminal 1 - Backend Server:
cd "e:\4th semester tasks\FED-LAB-FINAL\server"
npm start

Backend will run on: http://localhost:5000
✅ You should see: "MongoDB Connected Successfully" and "Server is running"


#### Terminal 2 - Frontend Server:
cd "e:\4th semester tasks\FED-LAB-FINAL\client"
npm start

Frontend will run on: http://localhost:3000
✅ Browser should automatically open the app


### Method 2: PowerShell (Both at Once)

Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd 'e:\4th semester tasks\FED-LAB-FINAL\server'; npm start"
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd 'e:\4th semester tasks\FED-LAB-FINAL\client'; npm start"


## 🔧 STEP 3: VERIFY MONGODB CONNECTION

The MongoDB connection is already configured with your credentials:
- Username: 233599_db_user
- Password: IeD2RhYF7uRyeJfZ
- Database: SmartLibrary

Connection string is in: server/.env

If connection fails:
1. Check internet connection
2. Verify MongoDB Atlas cluster is active
3. Check firewall settings


## 🧪 STEP 4: TEST THE APPLICATION

### Test 1: Add a Book
1. Fill in the form with:
   - Title: "Clean Code"
   - Author: "Robert C. Martin"
   - ISBN: "978-0132350884"
   - Year: 2008

2. Click "Add Book to Library"
3. ✅ You should see success message
4. ✅ Book appears in the list below


### Test 2: View Books
1. Check that all books are displayed in cards
2. Each card shows: Title, Author, ISBN, Year
3. ✅ Books should be sorted by newest first


### Test 3: Delete a Book
1. Click "Delete" button on any book card
2. Confirmation dialog appears
3. Click "Yes" to confirm
4. ✅ Book is removed from the list


### Test 4: Responsive Design
1. Open browser DevTools (F12)
2. Toggle device toolbar (Ctrl+Shift+M)
3. Test these screen sizes:
   - Mobile: 320px, 375px, 425px
   - Tablet: 768px
   - Desktop: 1024px, 1440px
4. ✅ Layout should adapt properly


## 📋 STEP 5: API ENDPOINT TESTING (Optional)

### Using Browser or Postman:

GET All Books:
http://localhost:5000/api/books

POST New Book:
URL: http://localhost:5000/api/books
Method: POST
Headers: Content-Type: application/json
Body:
{
  "title": "JavaScript: The Good Parts",
  "author": "Douglas Crockford",
  "isbn": "978-0596517748",
  "year": 2008
}

DELETE Book:
URL: http://localhost:5000/api/books/{book_id}
Method: DELETE


## 🐛 TROUBLESHOOTING

### Backend won't start:
- Check if port 5000 is available
- Verify MongoDB connection string in server/.env
- Run: npm install in server directory

### Frontend won't start:
- Check if port 3000 is available
- Run: npm install in client directory
- Clear cache: npm start -- --reset-cache

### "Failed to fetch books" error:
- Ensure backend server is running on port 5000
- Check browser console for CORS errors
- Verify MongoDB connection is successful

### MongoDB connection error:
- Check internet connection
- Verify credentials in server/.env
- Check MongoDB Atlas IP whitelist (should allow all: 0.0.0.0/0)


## 📸 TAKING SCREENSHOTS FOR SUBMISSION

1. Home page with empty library
2. Add book form filled with sample data
3. Book list showing 3-5 books
4. Delete confirmation dialog
5. Responsive view on mobile (320px)
6. Backend API response in Postman/Browser


## 📦 CREATING SUBMISSION ZIP

1. Stop both servers (Ctrl+C in terminals)

2. Create folder: rollnum_YourName

3. Copy these files/folders:
   - client/ (entire folder)
   - server/ (entire folder)
   - README.md
   - .gitignore
   - GITHUB_LINK.txt
   - package.json

4. Add screenshots folder with output images

5. Zip the folder: rollnum_YourName.zip


## 🎓 SUBMISSION CHECKLIST

✅ Complete source code (frontend and backend)
✅ README.md with setup instructions
✅ Output screenshots included
✅ GitHub repository link in GITHUB_LINK.txt
✅ .gitignore excludes node_modules
✅ All dependencies listed in package.json
✅ MongoDB connected with provided credentials
✅ All features working:
   - Add book
   - View books
   - Delete book
   - Responsive design
   - Error handling


## 🔗 USEFUL COMMANDS

# Check if services are running
netstat -ano | findstr :5000    # Check backend
netstat -ano | findstr :3000    # Check frontend

# View backend logs
cd server
npm start

# View frontend in browser
http://localhost:3000

# Test API
http://localhost:5000/api/books


## 📞 SUPPORT

If you encounter any issues:
1. Check console for error messages
2. Verify all dependencies are installed
3. Ensure MongoDB connection is active
4. Check that both servers are running


Good luck with your exam! 🎓
